package com.local.glucotracker.ui.design

import androidx.compose.ui.graphics.Color
import com.local.glucotracker.ui.design.tokens.GTLightColors

object FoodBrandTokens {
    // Kept under the old property name to avoid churn in snapshot fixtures.
    // Tour 6 returns the food flavor to the shared editorial palette.
    val Tangerine: Color = GTLightColors.ink
}
