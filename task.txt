Continue the Glucotracker glucose calibration work.

Current status:
- Backend has estimate_bias_at(t) using weighted median of nearby fingerstick residuals.
- Dashboard response includes per-point bias_confidence, nearest_fingerstick_distance_min, contributing_fingerstick_count.
- UI tooltip/summary now says “Смещение в этот момент”.
- Reports are not yet using pointwise normalized glucose.
- Bias-over-sensor-lifetime graph is still pending.

Task:
Add a visual bias-over-sensor-lifetime graph and improve the glucose tooltip/debug visibility.

Goals:
1. Make the time-varying bias model understandable to the user.
2. Make it obvious that old CGM points use old/local bias, not the current correction.
3. Do not change raw CGM.
4. Keep all wording informational, not medical.

Backend:
Add data to the glucose dashboard response or a dedicated endpoint for the current sensor:
- sensor age axis points
- fingerstick residual points:
  - measured_at
  - sensor_age_hours
  - fingerstick_value
  - raw_cgm_value
  - residual = fingerstick - raw
  - included/excluded
  - exclusion reason if excluded
- estimated bias curve sampled over the selected period or full sensor lifetime:
  - timestamp
  - sensor_age_hours
  - bias
  - confidence
  - contributing_fingerstick_count
  - nearest_fingerstick_distance_min
- phase markers:
  - warmup: 0–48h
  - stable
  - end_of_life if applicable

Frontend:
On `/glucose`, add a compact section in the right panel or below the main graph:

Title:
“Смещение по времени сенсора”

Content:
- small line chart
- x-axis: sensor age, e.g. “0ч”, “12ч”, “24ч”, “48ч”, “7д”, “14д”
- y-axis: bias in mmol/L
- orange diamond points: fingerstick residuals
- black/gray line: estimated bias_at(t)
- shaded warmup band: first 48h
- current time marker

If there are too few points:
Show empty state:
“Недостаточно записей из пальца для графика смещения.”

Tooltip on this bias chart:
- time / sensor age
- residual or estimated bias
- fingerstick value
- raw CGM value
- included/excluded
- confidence

Main glucose graph tooltip:
Make sure every CGM point tooltip shows:
- Raw CGM
- normalized value if available
- “Смещение в этот момент”
- confidence
- contributing fingerstick count
- nearest fingerstick distance

Copy:
Use:
- “Смещение в этот момент”
- “Оценка по записям из пальца”
- “Raw CGM сохраняется без изменений”
- “Автокалибровка 0–48ч”

Do not use:
- “истинная глюкоза”
- “правильная глюкоза”
- “медицинская калибровка”
- “рекомендовано”

Acceptance criteria:
- User can visually see bias changing over sensor lifetime.
- Old points clearly use local/old bias.
- Main tooltip exposes per-point bias metadata.
- UI handles no/low fingerstick data gracefully.
- Existing dashboard still works.
- Tests/build pass.

Run:
cd backend && .\\.venv\\Scripts\\python.exe -m pytest -q
cd backend && .\\.venv\\Scripts\\python.exe -m ruff check .
cd desktop && npm test -- --run
cd desktop && npm run build